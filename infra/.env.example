# Daily System — Environment Variables
# Copy to .env and customize for your environment

# PostgreSQL
POSTGRES_USER=daily
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=daily

# API
API_DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:7103/${POSTGRES_DB}
