// This file is intentionally minimal for the offline alpha.
// Full unit tests will be added as the app matures.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('basic test', () {
    expect(1 + 1, equals(2));
  });
}
